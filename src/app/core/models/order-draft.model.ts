/**
 * Placeholder shape for a future order form (Phase 2). No API calls in Phase 1.
 */
export interface OrderDraft {
  readonly id?: string;
  readonly contactEmail?: string;
  readonly notes?: string;
}
